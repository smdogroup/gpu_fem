This is a picture of the subdomains generated by my unstructured algorithm for BDDC (on a structured quad mesh). It seems to be having very snaky elements. I want to prevent that and make it more boxy so that if possible there is always at least two elements distance between outer subdomain boundaries. Right now that is not true - and I suspect that impacts thin shell performance a lot. Do you agree?

* Dr. K also said to try and add vertices if any subdomains are missing them.
* May also want to keep adding elements until we ensure no single-element boundary distances in each subdomain (if possible)..