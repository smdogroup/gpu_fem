Goal of this folder is to make a general toolset for debugging and analyzing the coarse-fine prolongation, nonlinear multigrid cycles / steps
and improving the nonlinear multigrid solves (for robustness).
Want to be able to compare plate, cylinder + wing cases (but starting with just the plate and wing cases first).